import { OnGoingFilmPipePipe } from './on-going-film-pipe.pipe';

describe('OnGoingFilmPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OnGoingFilmPipePipe();
    expect(pipe).toBeTruthy();
  });
});
