import { DashesToSpacesPipe } from './dashes-to-spaces.pipe';

describe('DashesToSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new DashesToSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
